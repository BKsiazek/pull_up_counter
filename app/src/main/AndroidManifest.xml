<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.barts.pullupcounter.pullupcounter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pull_up_icon_almost_white"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pull_up_icon_almost_white"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.ItemListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.DetailsActivity"></activity>
    </application>

</manifest>